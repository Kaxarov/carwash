# Car wash Crm project

Quick set up

create postgres database

config/local_settings.example.py rename -> local_settings.py

set database

set virtualenv

pip install -r requirements.txt

python manage.py migrate
